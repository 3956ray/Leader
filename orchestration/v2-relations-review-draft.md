# Explicit relations — next CP3 preparation, NOT DISPATCHED

Authority: approved emulator-first addendum E8 R1-R5. Current CP5 must close before dispatch. Bind baseline to accepted final CP5 commit, not an in-progress checkout. One developer, one Checkpoint. No AI provider setup needed.

Implement manual undirected related links between stable IDs of actual notes in the single current SQLite store. No duplicate bodies/demo data; no AI-created or same-category-inferred confirmed links. Edge stableID/endpoints/manual source/time. Reject self-links, duplicate unordered pairs, missing/recycled endpoints. Explicit create/remove UI; removing link never deletes note.

Default note list remains entry. Accessible related-note list opens exact destination, supports removal, honest empty/search states, text labels and usable large-font/TalkBack controls. Similar titles must still navigate correctID. Graph optional; no fake graph entry or graph completion claim if absent. If graph exists, same source/counts/filter boundaries as list and accessible list fallback.

Soft delete hides relationships in normal views but preserves identities; restoring originalID restores valid related views. Rename/category changes reflect current data, no stale copied title. Transaction failure must not show success, restart consistent, schema migration preserves notes/drafts/AI provenance/calendar/reminders. Backup format compatibility remains laterCP8; disclose relation exclusion until then.

Android tests actual durable synthetic notes: both create directions and duplicate rejection, self/missing IDs, remove without deleting records, title/category edit, soft-delete both endpoints/restore, process restart, accessibility navigation. At least1000notes/5000edges with bounded query/page/rendering, actual measured timing and environment. Verify DB endpoint sets against shown list and selected note. No whole-library graph render; report actual paging/truncation, not fake performance. Necessary host constraints/rollback/migration and affected existing regressions plus offline build/lint. Exact source/APK/evidence identities and clean Git boundaries.

No voice controls/models, provider AI request, CP8 format expansion, cloud/accounts/sync/payment/publishing/private data. Keep voicequality/provider/Xiaomi/family remaining conditions explicit. Next E9 backup compatibility only after accepted relation engineering.
