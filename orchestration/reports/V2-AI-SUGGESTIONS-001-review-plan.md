# V2-AI-SUGGESTIONS-001 review plan

Baseline84f3691f97efaae6e9fdbb7c81405e6525770e38. Attemptb14de6bf-bb39-4bae-a6b9-14953ec4a1ae, contractb9af9a330483d17c1e0bc6961a96201a04048fb752635cca3ed50c613477b06f. ACK and active execution01a0a879-98b5-73f0-87c8-38223331e59d recorded. Review the frozen contract I1-I6; prior preparation is orchestration/v2-ai-review-draft.md.

## Concrete boundary checks

Default-off and zero requests on startup/edit/save/import; opening settings never tests network automatically. User-confirmed endpoint/model/purpose and exact outgoing fields must match immutable request snapshot. Configuration or credentials change invalidates relevant consent; redirect must not forward bearer credentials or note text to an unconfirmed destination. Password masking, Android Keystore storage, no secret in logs/database/backup/evidence; disabling cancels, clearing removes persisted secret and invalidates in-flight work. Avoid weakening product TLS for test fixtures.

Current note body is not automatically minimal data: imported calendar text contains source details. Inspect text selection/preview and actual captured payload, including category labels/IDs, for unrelated notes/account/provenance leakage. Category names and note text are untrusted prompt content, never action authority. Strict response output bounds, title/category only; unknown category must require explicit creation confirmation.

RecordId/revision and title/category manual ownership must remain bound at request and at acceptance. Test switching note, editing draft, manually changing title/category, deleting/renaming target category, cancel/disable/clear/reconfigure, late success after timeout and duplicate submit. Accepting one suggestion must not silently make the second unsafe or unusable; deliberate independent accept flow should work while preserving manual edits. Original body unchanged; accepted provenance distinguished from final value.

Transport tests cover real Android request path with explicitly synthetic service, actual payload capture, invalid auth/errors/429/5xx/oversize/invalidUTF8/empty/injected response, no-network saving and restart defaults. Mock success is not provider_verified. No personal credentials currently available; do not fetch unrelated secrets or send live service requests. Provider condition remains explicit and does not block independent local engineering.

Safe migration preserves existing data/IDs. Current backup exclusions disclosed; credentials and temporary unaccepted responses excluded. Inspect new INTERNET permission with default-off/no automatic network invariants. Final APK/commit and test artifact identities, existing feature regressions, exact logs and explicit Xiaomi/family/provider gaps required before verdict.

## Initial implementation inspection
AiProtocol/AiJson now exist on codex/v2-ai-suggestions. Request is bounded8KiB selected text/32KiB envelope, up to50category candidates; response64KiB and timeout20s intended. Endpoint requires HTTPS chat/completions path, no userinfo/query/fragment/percent encoding; config.toString redacted. Response parser rejects duplicate keys, trailing data, malformed Unicode, excess nesting, tools/refusals and unexpected proposal fields; returns bounded title/category proposal only. This is source inspection, not execution or provider compatibility proof. Credential storage, transport and user consent/application flow still awaiting implementation. No finding from these initial two files requiring a separate dev interruption.

Transport/Vault initial review: HttpsURLConnection POST with redirects disabled, no retry/custom trust,5sconnect/10sread/20sdeadline and64KiBresponse. Cancellation flag checked around I/O; diagnostics map to codes. AiVault AES-GCM AndroidKeyStore encrypts settings including credential into noBackupFilesDir using AtomicFile, no plaintext fallback. Schema6 stores accepted provenance separately and explicit accept writes draft/category/provenance atomically. Await full model/UI consent and concurrency tests. Sent in-scope reminder to test independently accepting both fields without treating own acceptance as unrelated manual conflict, while actual manual edits invalidate pending proposal.

Concrete first model/UI finding: Dialog onDismissRequest=close bypasses saving-disabled close button. During acceptAi, close increments generation; callback token mismatch skips saving=false, leaving AI permanently busy until restart. Requested model+Dialog coherent dismissal guard and delayed-storage Back/close regression. Configuration-save dismissal must also respect pending state. Independent field acceptance now updates anchor on own successful write; still awaits actual ordered/manual-conflict tests.

Saving-dismiss source fix inspected: AiModel.close/cancel return while saving, covering Dialog system Back through close. Await delayed actual UI test. Eight host tests now cover destination/header rules, explicit outgoing payload, strict proposal/parser, both acceptance orders, manual intervening edits/obsolete category, atomic new-category+draft+provenance rollback, schema5→6. Tests not yet observed executed. Existing calendar permission assertion now expects INTERNET, appropriate to new explicit AI feature; still rejects WRITE_CALENDAR. Prior schema fixtures updated for6 without weakening row-preservation assertions. Backup-exclusion test currently only checks data() nonnull; final review must inspect actual serialized backup exclusions and credential paths, not infer from test name.

Observed107host0fail and build3/lint success41s. Four actual Android syntheticHTTPS tests passed: UI13.83s, TLS/errors/cancel12.956s, ownership10.506s, vault2.349s. Inspected16capturedrequests: all/v1/chat/completions, no unrelated PRIVATE_ACCOUNT_SENTINEL, logs store authorization_present only, not credential. Test-only socketFactory trusts only fixturecertificate and leaves hostname verification enabled; default system trust rejection and wronghostname tested. Requested total20s deadline slow-trickle case because sleep22s only triggers10s readtimeout; delayedBack,401/403/config-change and serialized backup checks still pending.

Resumed execution01a0a9f5-8e3b-7723-be84-8e307198e5d8 after confirmed usage-limit failure and explicit user continuation. Same contract/attempt, ownership retained. Verified8device logs OK, excluding separately retained previous delayed-UI-binding failure. Supplemental evidence includes401/403, config-change consent invalidation, reject/newcategory confirmation, serialized backup exclusions, delayed transactionBack protection and global deadline20030ms with500ms trickle and worker termination. Before/after process-restart full DB JSON identical. Viewed accepted-panel screenshot; native capture may precede final Compose redraw, so use assertions/DB for applied state rather than screenshot alone. Still await final commit/APK, actual configuration status screenshot, regressions and independent finalhost rerun.
