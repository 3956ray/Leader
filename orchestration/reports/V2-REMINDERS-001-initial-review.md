# Reminder initial code review

Status: in-progress source review, not acceptance.

Reviewed emerging ReminderRule, ReminderRepository, ReminderSchema and ReminderEngine while the sole developer task was authoritatively active. No tests or product files changed by commander.

Finding sent to developer: in ReminderEngine.fire, a crash after outcome POSTED commits but before finishOnce COMPLETED leaves requested=true. On restart reconcileOne sees no next occurrence and unconditionally records MISSED. RESERVED recovered as UNKNOWN has the same incorrect plan-level result. Require final one-time status to follow the current revision's persisted outcome, preserving POSTED, UNKNOWN, POST_FAILED and actual MISSED separately. Add crash-window recovery tests. Recurring schedules must still advance without reposting the uncertain occurrence.

This is feedback within V2-REMINDERS-001, not a new dispatch. Verification pending implementation and tests.

Follow-up source inspection: developer replaced unconditional MISSED with repository.terminalStatus and added UNKNOWN UI wording. ReminderTest now includes crash before notify, post-before-ack recovery for daily continuation, and POSTED-ack-before-once-finish recovery. These source changes address the identified state distinction; test execution and final Android integration remain unverified. Also inspected note schema migration to version 3 and transactional soft-delete invalidation (requested off, revision increment, next occurrence cleared), with restore keeping the plan disabled.

Android adapter review: AndroidReminderPort reuses reminder ID/tag and notification ID 1 for each cycle while setting onlyAlertOnce=true. Requested developer to verify and correct the case where a prior notification remains visible and a new occurrence updates it without alerting again. Each new valid occurrence must remain a real repeated reminder while identical occurrence callbacks remain deduplicated by the ledger. Requested isolated emulator evidence without dismissing the prior notification, clearly labeled compressed-time engineering evidence.

Activity integration review: MainActivity.onCreate unconditionally invokes COLD reconciliation, including rotation/font/theme recreation with the process runtime still alive. Current engine reconciliation skips an overdue unhandled occurrence, so ordinary recreation during permitted inexact-alarm delay can discard an otherwise pending valid callback. Requested true process-start versus Activity-recreation distinction and an Android test preserving that pending occurrence. Genuine cold-start missed recovery remains governed by B9. Also requested notification deep-link testing while another note has unsaved edits, verifying durable draft preservation.

AndroidReminderTest draft review: missing-note intent currently lacks UI outcome assertions; its comment promises a separate activity without implementing it. Existing deep-link scenario does not start with another unsaved note. Requested explicit deleted/missing UI assertions and A-draft to B-notification to A-preservation coverage. New-cycle notification postTime and flag assertions prove update/flags, not audible or haptic delivery; requested system alert evidence or honest observation limits. Ordinary notification now sets onlyAlertOnce=false (summary still true), addressing adapter suppression at source level; runtime verification remains pending.

Subsequent source review confirms AndroidReminderTest now asserts preserved A draft after notification opens B, deleted/missing-note messages with no body editor, and Activity recreation preserving an injected overdue scheduled occurrence before manual callback. The latter is explicitly synthetic timing, not real delayed OS delivery. MainActivity now calls runtime.activityStart, guarded once per process runtime; ordinary Activity recreation no longer reruns COLD. Actual test outcomes still await the running emulator suite.

Emulator test progress: a single real OS alarm produced persisted POSTED/notify_returned, with scheduled time 1789495380000 and checked time 1789495389694 (9.694 seconds delay). This is one observed delivery only. The full AndroidReminderTest has not passed: developer identified notification navigation creating a second Activity, leaving the original editor behind and preventing recreation checks from continuing. Developer is fixing task reuse and will rerun the scenario. Do not accept deep-link/draft preservation from the partial run.

Correction to the preceding initial diagnosis: subsequent ActivityTaskManager logs show notification launch result code 3 (existing Activity intent delivery), so a second Activity was not established by evidence. The second run still failed ActivityScenario recreation with PAUSED lifecycle state. Developer now attributes this to MainActivity.setIntent changing the launch-intent identity used by ActivityScenario lifecycle filtering, and is adjusting only the test's pre-recreation intent identity. Preserve the original failure logs and this correction; do not report the initial second-Activity hypothesis as a proven product defect. Actual recreation and A/B content assertions must still execute, not be removed or replaced by a mock.

Third run: `/private/tmp/thinkv2-reminders-os3.log` reports OK (1 test). Reviewed test retains actual AlarmReceiver delivery, private notification content assertions, A-draft/B-note navigation, actual recreation with test-only intent identity reset, missing/deleted UI outcomes, later compressed cycles with old notification retained, and delayed-callback preservation across recreation. This is interim Android system-flow evidence; reboot/timezone/update evidence, final build binding and commander independent verification remain outstanding.

Recovery and visual follow-up: verified BOOT, TIME_CHANGED, two TIMEZONE_CHANGED snapshots and PACKAGE_REPLACED. New York/Taipei retain ID/revision and local 09:00 while UTC epoch differs by 12 hours. Cold-link sampled PENDING, followed by a queued BOOT job and SCHEDULED; a repeated force-stop/launch also ends SCHEDULED with BOOT last. This proves observed cold/system recovery convergence, not an isolated device COLD-only execution. Do not repeat restarts solely to force a last-reason label. Commander independently reran all 52 host tests successfully with 53 input hashes unchanged. Viewed corrected reminder-list.png (actual list/SCHEDULED card), reminder-form.png (real local date/time and enabled daily form), and cold-link.png (correct synthetic note). Final OS alarm artifact, source/commit/APK binding and clean diff remain to be checked at receipt.

Final evidence review: os-alarm-excerpt.txt contains the scheduled RTC_WAKEUP at epoch 1790125200000, matching the Taipei plan. Final rerun of prior Android regression hit a category-picker timing failure; inspected NotesUiTest change only closes keyboard, waits for scroll idle and waits for the actual new-category control before clicking. Assertions remain intact. Product/host input hashes remain unchanged, so independent 52-host-test result still applies. Developer will preserve the OS-test APK separately from the final test APK after this test-only synchronization change, and rerun affected regression before submission.
