# JNA 5.18.1 AAR proposed acquisition and static inspection

Status: concrete proposal, not dispatched or authorized. Sole next object; CP2 only.

Object: net.java.dev.jna:jna:5.18.1, type/extension aar, no classifier.
URL: https://repo.maven.apache.org/maven2/net/java/dev/jna/jna/5.18.1/jna-5.18.1.aar
Basis: accepted Vosk 0.3.75 module selector and Maven Central layout. Actual existence, bytes, SHA and internal contents remain unknown. Record SHA after acquisition; do not portray it as independent publisher verification.

Proposed executor: existing developer task under Leader. One isolated new attempt directory; preserve all existing evidence. At most two GET attempts, no automatic redirects or retries, HTTPS only, 64MiB per response/128MiB total, 180 seconds per attempt. Only a transient transport failure may consume the second attempt; permission/TLS/identity/resource failure stops. Whole task at most two hours.

Inspect using already available standard archive and JDK/LLVM static tools under the existing simplified safety process. Check archive paths, links, duplicates, entry types and declared/actual resource bounds before limited extraction; no extractall or archive-provided program execution. At most 10000 outer entries, 512MiB total expansion, 128MiB single member, one classes.jar nesting level; text 2MiB/file and 32MiB total. Stop on unsupported or unsafe structures rather than extending scope.

Inventory Java/native members and ABI identities, package LICENSE/NOTICE/third-party attribution, native-loading entry points and relevant memory API signatures. At most 20 static tool calls, 120 seconds each, 16MiB output/call and 128MiB total. No exhaustive call-graph/source closure. Missing coverage stays Unknown.

Deliver retained request/response and hashes, inventory, bounded static evidence, per-object verdict and explicit remaining admission issues. Leader independently verifies before acceptance. No installation, Gradle/Maven build, automatic dependency fetching, target native loading, microphone, real-device execution, product integration, new source/sidecar/second dependency, or old package reacquisition. Original audio privacy and all Checkpoint gates remain unchanged.

Approval would authorize only acquisition and offline static inspection of this one fixed AAR. It would not approve adoption or execution. PM formal scope alignment and any necessary product-document sync occur before developer dispatch; routine alignment is Leader-managed within the approved scope.
