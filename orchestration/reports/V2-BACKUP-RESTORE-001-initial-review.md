# Backup initial source review

Status: active implementation, not acceptance.

Reviewed first StrictJson.kt: bounded input/output, strict UTF-8 decoder, duplicate object keys, integer parsing, surrogate handling and streamed base64 decoding. No tests have been reviewed or run yet.

Finding sent within existing CP8 task: Unicode escape hex parsing uses Character.digit, which accepts non-ASCII digit forms although JSON escapes require ASCII 0-9/a-f/A-F. Require ASCII-only escape digits and a rejection test, retaining legitimate Chinese/emoji round-trip coverage. Final parser/schema/merge/SAF review remains pending.

Follow-up: source now restricts escape digits to ASCII ranges; BackupCodecTest rejects fullwidth digits/letters and preserves valid emoji surrogate pairs. Reviewed BackupData's explicit envelope fields, exact-byte hash, note/draft/category/reminder/origin representation, empty future relations, reference and draft-base checks. Initial codec tests cover synthetic draft-only/trash/tombstone Unicode round-trip, checksum whitespace identity, malformed/unknown/duplicate/truncated input, count/depth/byte bounds and output failure. Real database snapshot/merge and SAF are still pending; model fixtures alone do not prove compatibility with every current repository state.

Reviewed first BackupRepository: consistent snapshot in transaction, whole-record bundle comparison, category conflict mapping, local fingerprint recheck, transaction-only insertion and disabled imported reminders. Finding sent: copyName uses UTF-16 take(50), which can split an emoji surrogate pair in a valid category name. Require code-point truncation and an actual conflict-merge then re-export test with 49 ASCII characters followed by an emoji. Final integration and tests remain pending.

Follow-up: copyName now truncates by code point; the requested category conflict/re-export test is present. Reviewed 10 real-SQLite repository tests covering full restore (disabled reminders excepted), source unchanged, trash/inactive category/discard watermarks, whole-record conflict mapping, stale preview after relevant mutations, injected failure rollback, idempotent receipts and local enabled reminder preservation. First JUnit discovery failed on a non-void test signature; corrected run reports all 10 repository tests and all 10 codec tests passing. Interim XML identities recorded separately. Android SAF, error UI and final independent review remain pending.

Storage/UI-model review: output is closed then reread and checksum verified before success naming, with error cleanup. Requested C4 correction: a suggested .partial filename is editable in the system picker and may be transformed by a provider. Verify/establish the actual incomplete filename before writing bytes, then rename only after verification. Cover edited filename plus interrupted/failing write, and restrict cleanup to the newly created export target. Final integration and real SAF behavior are not yet verified.

BackupScreen integration review: preview clearly distinguishes merge, conflicts, category mapping, disabled imported reminders, and receipt consumption including skipped items. Found result-list issue: apply includes inactive draft-only watermark IDs in importedIds, causing an unopenable unnamed-draft entry because NoteRepository.open returns null. Requested preserving these internal watermarks while excluding them from actionable note rows and visible record counts, with a discarded-draft restore scenario. Developer reports 72 host tests and ongoing build/SAF verification; final evidence still pending.

Writer follow-up: extracted BackupWriter checks actual provider filename, renames and rechecks .partial before writing, closes and rereads through strict codec, then finalizes the name. Cleanup follows the returned renamed handle. Reviewed tests for edited filename ordering, refused/ignored rename with zero writes, partial space failure, close failure and corrupt readback. BackupModel now filters inactive draft-only IDs from result links. Android provider and UI verification still required.

Android workflow interim review: before-export.json and after-export-cancel.json are byte-identical (SHA-256 6b7afda3e857b76d3411afb0521ef5bf5378673a16b8a367f3092692ba07d50b). This proves cancellation preserved the captured database tables only. Subsequent export-picker/export-result captured a test APK crash dialog; filename/SAVE commands failed assertions. Successful export is not demonstrated by those files. Developer notified within the existing task to diagnose the test provider crash and repeat affected SAF workflows. No acceptance yet. Reviewed additional trash-card conflict marker; latest visible-count/UI changes postdate the independent 75-test run, whose final identity remains pending.

Provider crash follow-up: independently read /private/tmp/thinkv2-backup-provider-crash.log. Stack identifies com.example.thinkv2.test and BackupTestDocumentsProvider.queryRoots with missing kotlin.Pair. Reviewed replacement Java-only test provider: platform MatrixCursor/SharedPreferences/proxy descriptor, synthetic ENOSPC writes, test-manifest-only registration, no added dependencies. Test APK rebuild/lint log reports success. Actual repeated SAF success/error evidence still pending; prior screenshots correctly excluded from success evidence.

Post-provider-fix SAF review: actual Downloads export with edited backup-user.thinkbackup.json name now returns product export-not-completed/partial-cleaned UI, without the test-process crash. Successful local export remains unproven. Requested stage-specific diagnosis of actual provider filename normalization and renamed URI permissions, retaining Downloads coverage. Confirmed separate UI defect: BackupScreen displays repreview for every error, including export failure with no import candidate. Sent scoped correction to gate repreview on a valid retained candidate.

Phase-specific rerun: export-phase-failure reports incomplete-name handling failure; repreview action is absent. Baseline C4 inspected: unfinished output must not be named/presented as successful, but literal .partial suffix is not a PM requirement. Sent implementation guidance to permit an unmistakable incomplete name compatible with JSON MIME normalization, once actual provider metadata confirms cause; preserve verification-before-final-name and update format docs/tests. No product-scope escalation needed for this naming compatibility choice.

Prefix implementation review: Writer now checks a Chinese incomplete prefix before writing, with JSON-normalization test. New actionable finalization defect: final check only tests .thinkbackup.json suffix, so an ignored final rename leaves the incomplete prefix yet reports success. Requested actual final-name marker removal validation and a test where initial rename succeeds but final rename is ignored. Acceptance remains pending.

Final-name finding source follow-up: current Writer strips repeated leading incomplete markers for desired name and rejects a final actual name retaining the marker. Reviewed ignoredFinalRenameCannotReportSuccess (failure phase/cleanup) and repeatedMarkersAreRemovedAndProviderUniqueNameIsAccepted tests. Actual system chooser export/restore remains pending; shell-permission metadata probe is diagnostic only, not proof of app URI grants.

Actual name probe did not establish MIME normalization: saf-name-probe.txt contains only initial created name; AndroidJUnit log shows 1 test/1 failure at post-rename name query (empty cursor). Shell command exit 0 is not a passing instrumentation result. Notified developer to diagnose returned document identity/query visibility from synthetic metadata and avoid claiming an unproven suffix root cause.

Correction backed by Downloads child query: document IDs msf:20/21/22 still identify backup-user.thinkbackup.json.partial.json, backup-phase.thinkbackup.json.partial.json and saf-probe.thinkbackup.json.partial.json. This confirms suffix normalization plus invalid returned identity for those attempts, and contradicts the earlier UI cleanup claim. Delete return true alone did not prove cleanup. Database unchanged evidence remains valid; filesystem cleanup is NOT accepted. Developer is correcting cleanup certainty and gathering prefix-scheme actual URI/name evidence.

Cleanup correction source reviewed: Writer now requires readable current identity before delete, true delete return, and missing identity afterward for removedPartial=true; unreadable renamed identities report uncertainty. Latest test XML totals: 79 tests, 0 failures/errors. This is developer-run evidence; final independent identity/tests pending.

Second probe evidence: new document numeric ID 15 became actual msf:23 after suffix rename; .partial.json normalization observed. Prefix rename succeeds on recovered msf:23 and probe reports no own leftovers. This does not prove prefix rename/finalization directly from a newly created numeric ID, so requested actual SAF coverage for default and user-edited filenames. Diagnostic evidence is not substituted for app permissions or successful exports.

Normal SAF progress: reviewed prefix-export-result.png visually; user-edited backup-valid.thinkbackup.json reports written/verified completion with privacy guidance visible. Independently compared before-export.json and after-prefix-export.json: identical parsed database snapshots. This is UI/source-preservation evidence only; actual exported bytes, default-name path and restore/error workflows still pending.

First SAF ENOSPC attempt is not ENOSPC evidence: enospc-result reports incomplete-name-stage failure/uncertain cleanup, not write-close. Requested test-provider evidence that openDocument/onWrite actually returns ENOSPC and rerun after diagnosing earlier failure. Source database snapshot remains unchanged.

Fault-provider API review: local Android SDK sources/android-37.0/android/provider/DocumentsProvider.java lines 279-283 require null when original document ID remains valid. Lines 1253-1268 grant returned URI then revoke original ID for nonnull returns. Test provider returning unchanged ID violates this contract and explains possible grant loss before ENOSPC. Sent evidence to developer to fix fixture and rerun actual SAF without changing product permissions.

Corrected SAF fault run reviewed: test provider now returns null for unchanged ID. enospc-events.xml has rename_same_id=1, open=1, ENOSPC=1; source confirms ENOSPC counter immediately precedes thrown error in write/fsync callback. UI now reports write/close failure and uncertain cleanup. after-enospc database equals before-export. Provider state retains only prior failed space-case document, no current space-fixed document; do not imply all historical leftovers cleared. This supports synthetic ENOSPC handling through actual SAF, not physical disk exhaustion. Restore/duplicate workflow remains pending.
