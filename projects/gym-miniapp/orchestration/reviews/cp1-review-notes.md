# CP1 进行中审阅

Task: GYM-CP1-IDENTITY-001
Status: 未验收；开发者仍在实现和补证据

## 已读与发现

已阅读身份SQL、严格JSON解析、微信适配器、会话/角色/限流事务、HTTP路由、角色CLI及原生登录页面和测试。当前改动未扩展会员、忙闲或课表业务。

1. 登录限流原在取得数据库写锁前采样时间，跨分钟等待可能选错窗口。已通知开发者并看到修复：锁内now()；新增测试以db.isTransaction区分边界时间，旧满桶到新窗口可登录。最终仍需独立复验。
2. 页面onHide取消expiryTimer但onShow保留旧authenticated直到请求结束，后台超过期限后可能短暂显示旧登录。已要求回前台立即待确认，并测试挂起/失败/401及隐藏后迟到响应。
3. 要求operator撤销与受保护写的两种提交顺序有实际并发证据；现有顺序角色测试不能单独代替此要求。

已有开发者identity-tests.log记录15项通过，尚不代表全部CP1或Leader验收。真实微信/工具/真机仍未验证。
